import React, { useState, useEffect, useRef } from "react";
import {
View, Text, TouchableOpacity, TextInput, StyleSheet, Animated, ScrollView, Modal
} from "react-native";
import AsyncStorage from '@react-native-async-storage/async-storage';

const IconeCinturao = ({ cor }) => (
<View style={styles.cinturaoContainer}>
<View style={[styles.cinturaoBase, { backgroundColor: cor === 'gold' ? '#FFD700' : '#E2E8F0' }]} />
<View style={[styles.cinturaoCircle, { backgroundColor: cor === 'gold' ? '#B7791F' : '#94A3B8' }]} />
</View>
);

const IconeMais = () => (
<View style={styles.iconeMaisContainer}>
<View style={styles.iconeMaisHorizontal} />
<View style={styles.iconeMaisVertical} />
</View>
);

const IconeEngrenagem = () => (
<View style={styles.iconeEngrenagemContainer}>
<View style={styles.iconeEngrenagemCentro} />
<View style={[styles.iconeEngrenagemDente, { transform: [{ rotate: '0deg' }] }]} />
<View style={[styles.iconeEngrenagemDente, { transform: [{ rotate: '45deg' }] }]} />
<View style={[styles.iconeEngrenagemDente, { transform: [{ rotate: '90deg' }] }]} />
<View style={[styles.iconeEngrenagemDente, { transform: [{ rotate: '135deg' }] }]} />
</View>
);

const IconeLixeira = ({ cor = '#FFF' }) => (
<View style={styles.iconeLixeiraContainer}>
<View style={[styles.iconeLixeiraTampa, { backgroundColor: cor }]} />
<View style={[styles.iconeLixeiraAlca, { borderColor: cor }]} />
<View style={[styles.iconeLixeiraCorpo, { borderColor: cor }]}>
<View style={[styles.iconeLixeiraLinha, { backgroundColor: cor }]} />
<View style={[styles.iconeLixeiraLinha, { backgroundColor: cor }]} />
</View>
</View>
);

const LogoUfcCiems = () => (
<View style={styles.logoContainerOuter}>
<View style={styles.logoMolduraEsquerda} />
<View style={styles.logoContainer}>
<View style={styles.logoUfcQuadrado}>
<Text style={styles.logoUfcTexto}>UFC</Text>
</View>
<View style={styles.logoDivisorVertical} />
<View style={styles.logoCiemsContainer}>
<Text style={styles.logoCiemsTexto}>CIEMS</Text>
<Text style={styles.logoSubTexto}>CHAMPIONSHIP</Text>
</View>
</View>
<View style={styles.logoMolduraDireita} />
</View>
);

const ItemLutadorDeslocavel = ({ nome, index, totalItens, onReorder, onEdit, onDelete }) => {
return (
<View style={[styles.rowGerenciarCard, styles.elementoLarguraPadrao]}>
<View style={styles.containerControlesDeslocamento}>
<TouchableOpacity
disabled={index === 0}
style={[styles.botaoSetaDeslocar, index === 0 && { opacity: 0.1 }]}
onPress={() => onReorder(index, -1)}
>
<Text style={styles.textoSeta}>▲</Text>
</TouchableOpacity>
<TouchableOpacity
disabled={index === totalItens - 1}
style={[styles.botaoSetaDeslocar, index === totalItens - 1 && { opacity: 0.1 }]}
onPress={() => onReorder(index, 1)}
>
<Text style={styles.textoSeta}>▼</Text>
</TouchableOpacity>
</View>
<TouchableOpacity style={styles.botaoGerenciarNome} onPress={onEdit}>
<Text style={styles.textoBotaoCardLutador}>{nome}</Text>
</TouchableOpacity>
<TouchableOpacity style={styles.botaoExcluirLutador} onPress={onDelete}>
<IconeLixeira cor="#FF0000" />
</TouchableOpacity>
</View>
);
};

export default function App() {
const [tela, setTela] = useState("menu_inicial");
const [menuVisivel, setMenuVisivel] = useState(false);
const [modoTreino, setModoTreino] = useState(false);
const [valeCinturao, setValeCinturao] = useState(null);
const [modalAbandonar, setModalAbandonar] = useState(false);
const [modalRetornarLuta, setModalRetornarLuta] = useState(false);
const [roundAcabouAviso, setRoundAcabouAviso] = useState(false);
const [modalAlerta, setModalAlerta] = useState({ visivel: false, titulo: "", mensagem: "", acaoOk: null });
const [modalConfirmarGeral, setModalConfirmarGeral] = useState({ visivel: false, titulo: "", mensagem: "", acaoSim: null });
const [modalExcluirConfirm, setModalExcluirConfirm] = useState(false);
const [lutadorParaExcluir, setLutadorParaExcluir] = useState(null);
const [modalOpcoesLixeira, setModalOpcoesLixeira] = useState(false);
const [lutadorSelecionadoLixeira, setLutadorSelecionadoLixeira] = useState(null);
const [modalRecuperarConfirm, setModalRecuperarConfirm] = useState(false);
const [modalApagarDefinitivoConfirm, setModalApagarDefinitivoConfirm] = useState(false);
const [historicoEventos, setHistoricoEventos] = useState([]);
const [lutadoresDB, setLutadoresDB] = useState({
CARANGUEJO: ["Isaac The Dablio", "Miguel Bolinha", "Gonçalves", "João Caralhão", "Charles William", "William Pretão", "Pedro Bellingham", "Arthur Jackson", "Bryan Brancão", "Kaleb Khabib"],
TUBARÃO: ["Arthur China", "Thallysson W", "Luís Adolfo", "Benio The Black", "Gabriel Vertssapen", "Enzo Dela Vega", "Luan Ologna", "Miguel Jesus"],
FEMININO: ["Nina Di Casteli", "Joana The Pig", "Mc Soares", "Antonella Di Marco", "Volkova", "Mary", "Charlotte"],
});
const [lixeiraDB, setLixeiraDB] = useState([]);
const [novoLutadorNome, setNovoLutadorNome] = useState("");
const [categoriaSelecionada, setCategoriaSelecionada] = useState(null);
const [lutadorSendoEditado, setLutadorSendoEditado] = useState(null);
const [categoriaAntigaEditado, setCategoriaAntigaEditado] = useState(null);
const [eventoEmAndamento, setEventoEmAndamento] = useState(null);
const [inputEventoEspecial, setInputEventoEspecial] = useState("");
const [idLutaAtiva, setIdLutaAtiva] = useState(null);
const [lutaAtualIdx, setLutaAtualIdx] = useState(null);
const [categoria, setCategoria] = useState(null);
const [lutador1, setLutador1] = useState(null);
const [lutador2, setLutador2] = useState(null);
const [rounds, setRounds] = useState(null);
const [roundAtual, setRoundAtual] = useState(1);
const [vida1, setVida1] = useState(200);
const [vida2, setVida2] = useState(200);
const [turnos1, setTurnos1] = useState(0);
const [turnos2, setTurnos2] = useState(0);
const [vezDeQuem, setVezDeQuem] = useState(null);
const [dano, setDano] = useState("");
const [alvoSelecionado, setAlvoSelecionado] = useState(null);
const [metodoSelecionado, setMetodoSelecionado] = useState(null);
const [resultado, setResultado] = useState(null);
const [itemSelecionado, setItemSelecionado] = useState(null);
const [vencDecisaoTemp, setVencDecisaoTemp] = useState(null);
const [pointsL1, setPointsL1] = useState("");
const [pontosL2, setPontosL2] = useState("");
const vidaMax = 200;
const vidaAnim1 = useRef(new Animated.Value(vidaMax)).current;
const vidaAnim2 = useRef(new Animated.Value(vidaMax)).current;
const [carregado, setCarregado] = useState(false);

const iniciarLuta = () => {
setVida1(vidaMax);
setVida2(vidaMax);
setTurnos1(0);
setTurnos2(0);
setRoundAtual(1);
setVezDeQuem(null);
setAlvoSelecionado(null);
setMetodoSelecionado(null);
setRoundAcabouAviso(false);
vidaAnim1.setValue(vidaMax);
vidaAnim2.setValue(vidaMax);
setTela("escolher_rounds");
};

const ordenarLutasDoEvento = (lutas, nomeEvento) => {
if (!lutas || !Array.isArray(lutas)) return [];
const nomeNormalizado = nomeEvento ? String(nomeEvento).toUpperCase().trim() : "";
return [...lutas].sort((a, b) => {
if (nomeNormalizado === "UFC MUNICH") {
const pesoA = a.cinturao && a.corCinturao === 'silver' ? 2 : (a.cinturao && a.corCinturao === 'gold' ? 1 : 0);
const pesoB = b.cinturao && b.corCinturao === 'silver' ? 2 : (b.cinturao && b.corCinturao === 'gold' ? 1 : 0);
return pesoB - pesoA;
} else {
const pesoA = a.cinturao && a.corCinturao === 'gold' ? 2 : (a.cinturao && a.corCinturao === 'silver' ? 1 : 0);
const pesoB = b.cinturao && b.corCinturao === 'gold' ? 2 : (b.cinturao && b.corCinturao === 'silver' ? 1 : 0);
return pesoB - pesoA;
}
});
};

const mostrarAlertaCustom = (titulo, mensagem, acao = null) => {
setModalAlerta({ visivel: true, titulo, mensagem: mensagem, acaoOk: acao });
};

const getLabelLutaPorArrayOriginal = (lutadorA, lutadorB) => {
if (modoTreino) return "TREINO";
if (!eventoEmAndamento || !eventoEmAndamento.lutasAgendadas) return "PRELIMINARY CARD";
const idxOriginal = eventoEmAndamento.lutasAgendadas.findIndex(
l => l.lutador1 === lutadorA && l.lutador2 === lutadorB
);
if (idxOriginal === 0) return "MAIN EVENT";
if (idxOriginal === 1) return "CO-MAIN EVENT";
return "PRELIMINARY CARD";
};

const getContagemLutas = (p1, p2) => {
let contador = 1;
historicoEventos.forEach(ev => {
if(ev && ev.lutasAgendadas) {
ev.lutasAgendadas.forEach(l => {
if ((l.lutador1 === p1 && l.lutador2 === p2) || (l.lutador1 === p2 && l.lutador2 === p1)) {
contador++;
}
});
}
});
if (eventoEmAndamento && eventoEmAndamento.lutasAgendadas) {
eventoEmAndamento.lutasAgendadas.forEach(l => {
if ((l.lutador1 === p1 && l.lutador2 === p2) || (l.lutador1 === p2 && l.lutador2 === p1)) {
contador++;
}
});
}
return contador > 1 ? contador : "";
};

const getProximoNumerado = () => {
const numerados = historicoEventos.filter(ev => ev && ev.nome && /^UFC \d+$`/.test(ev.nome));
if (numerados.length === 0) return 324;
const nums = numerados.map(ev => parseInt(ev.nome.replace("UFC ", "")) || 0);
return Math.max(...nums) + 1;
};

const getCartel = (nome) => {
let v = 0, d = 0, e = 0;
const todasLutas = historicoEventos.reduce((acc, ev) => acc.concat(ev.lutasAgendadas || []), []);
todasLutas.forEach(l => {
if (!l || !l.resultado) return;
if (l.lutador1 === nome || l.lutador2 === nome) {
if (l.resultado.metodo === "EMPATE") e++;
else if (l.resultado.vencedor === nome) v++;
else d++;
}
});
return ``${v}-{e}`;
};

useEffect(() => {
if (tela !== "luta") return;
Animated.timing(vidaAnim1, {
toValue: vida1,
duration: 600,
useNativeDriver: false
}).start();
if (vida1 <= 0) { finalizarLuta("TKO", lutador2, turnos2, roundAtual); }
}, [vida1, tela]);

useEffect(() => {
if (tela !== "luta") return;
Animated.timing(vidaAnim2, {
toValue: vida2,
duration: 600,
useNativeDriver: false
}).start();
if (vida2 <= 0) { finalizarLuta("TKO", lutador1, turnos1, roundAtual); }
}, [vida2, tela]);

useEffect(() => {
const salvarDados = async () => {
if (carregado) {
try {
const dados = JSON.stringify({ historicoEventos, eventoEmAndamento, lutadoresDB, lixeiraDB });
await AsyncStorage.setItem('ufc_dados', dados);
} catch (e) {
console.log("Erro ao salvar no AsyncStorage:", e);
}
}
};
salvarDados();
}, [historicoEventos, eventoEmAndamento, lutadoresDB, lixeiraDB, carregado]);

useEffect(() => {
const carregarDados = async () => {
try {
const dadosSalvos = await AsyncStorage.getItem('ufc_dados');
if (dadosSalvos) {
const dados = JSON.parse(dadosSalvos);
if (dados.historicoEventos) setHistoricoEventos(dados.historicoEventos);
if (dados.eventoEmAndamento) setEventoEmAndamento(dados.eventoEmAndamento);
if (dados.lutadoresDB) setLutadoresDB(dados.lutadoresDB);
if (dados.lixeiraDB) setLixeiraDB(dados.lixeiraDB);
}
} catch (e) {
console.log("Erro ao carregar dados do AsyncStorage:", e);
} finally {
setCarregado(true);
}
};
carregarDados();
}, []);

const handleAdicionarLutador = () => {
if(!novoLutadorNome.trim() || !categoriaSelecionada){
mostrarAlertaCustom("ERRO", "PREENCHA O NOME E SELECIONE UMA CATEGORIA!");
return;
}
const nomeNormalizado = novoLutadorNome.trim();
const existeAtivo = Object.values(lutadoresDB).some(catList => (catList || []).includes(nomeNormalizado));
if (existeAtivo) {
mostrarAlertaCustom("ERRO", "JÁ EXISTE UM LUTADOR ATIVO COM ESSE NOME!");
return;
}
setLutadoresDB(prev => ({
...prev,
[categoriaSelecionada]: [...(prev[categoriaSelecionada] || []), nomeNormalizado]
}));
mostrarAlertaCustom("SUCESSO", ${nomeNormalizado.toUpperCase()} ADICIONADO!`, () => {
setNovoLutadorNome(""); setCategoriaSelecionada(null); setTela("menu_inicial");
});
};

const handleSalvarEdicaoLutador = () => {
if(!novoLutadorNome.trim() || !categoriaSelecionada){
mostrarAlertaCustom("ERRO", "O NOME E A CATEGORIA NÃO PODEM FICAR VAZIOS!");
return;
}
const nomeNovo = novoLutadorNome.trim();
setLutadoresDB(prev => {
const copiaCategoriaAntiga = (prev[categoriaAntigaEditado] || []).filter(l => l !== lutadorSendoEditado);
let novoEstado = { ...prev, [categoriaAntigaEditado]: copiaCategoriaAntiga };
novoEstado[categoriaSelecionada] = [...(novoEstado[categoriaSelecionada] || []), nomeNovo];
return novoEstado;
});
mostrarAlertaCustom("SUCESSO", "LUTADOR ATUALIZADO!", () => {
setNovoLutadorNome(""); setLutadorSendoEditado(null); setCategoriaSelecionada(null); setCategoriaAntigaEditado(null);
setTela("gerenciar_lutadores");
});
};

const handleReordenarLutadores = (indexAntigo, direcao) => {
if(!lutadoresDB[categoria]) return;
const listaAtual = [...lutadoresDB[categoria]];
let novoIndex = indexAntigo + direcao;
if (novoIndex < 0 || novoIndex >= listaAtual.length) return;
const temp = listaAtual[indexAntigo];
listaAtual[indexAntigo] = listaAtual[novoIndex];
listaAtual[novoIndex] = temp;
setLutadoresDB(prev => ({
...prev,
[categoria]: listaAtual
}));
};

const acionarExclusaoLutador = (nome, catOrigem) => {
setLutadorParaExcluir({ nome, categoria: catOrigem });
setModalExcluirConfirm(true);
};

const confirmarMoverParaLixeira = () => {
if (!lutadorParaExcluir) return;
setLutadoresDB(prev => ({
...prev,
[lutadorParaExcluir.categoria]: (prev[lutadorParaExcluir.categoria] || []).filter(l => l !== lutadorParaExcluir.nome)
}));
setLixeiraDB(prev => [...prev, { nome: lutadorParaExcluir.nome, categoria: lutadorParaExcluir.categoria }]);
setModalExcluirConfirm(false);
setLutadorParaExcluir(null);
mostrarAlertaCustom("AVISO", "LUTADOR MOVIDO PARA A LIXEIRA.");
};

const confirmarRecuperacao = () => {
if (!lutadorSelecionadoLixeira) return;
const { nome, categoria } = lutadorSelecionadoLixeira;
setLutadoresDB(prev => ({
...prev,
[categoria]: [...(prev[categoria] || []), nome]
}));
setLixeiraDB(prev => prev.filter(l => l.nome !== nome));
setModalRecuperarConfirm(false);
setLutadorSelecionadoLixeira(null);
setModalOpcoesLixeira(false);
mostrarAlertaCustom("SUCESSO", "LUTADOR RECUPERADO COM SUCESSO!");
};

const confirmarApagarDefinitivo = () => {
if (!lutadorSelecionadoLixeira) return;
setLixeiraDB(prev => prev.filter(l => l.nome !== lutadorSelecionadoLixeira.nome));
setModalApagarDefinitivoConfirm(false);
setLutadorSelecionadoLixeira(null);
setModalOpcoesLixeira(false);
mostrarAlertaCustom("SUCESSO", "LUTADOR EXCLUÍDO PERMANENTEMENTE.");
};

const registrarTurno = (num) => {
setAlvoSelecionado(null);
let novosT1 = turnos1;
let novosT2 = turnos2;
if (num === 1) { novosT1 = Math.min(10, turnos1 + 1); setTurnos1(novosT1); setVezDeQuem(2); }
else { novosT2 = Math.min(10, turnos2 + 1); setTurnos2(novosT2); setVezDeQuem(1); }
if (novosT1 + novosT2 >= 20) {
setRoundAcabouAviso(true);
}
};

const processarProximoRound = () => {
setRoundAcabouAviso(false);
if (roundAtual < rounds) {
setRoundAtual(r => r + 1);
setTurnos1(0);
setTurnos2(0);
setVezDeQuem(null);
} else {
setTela("escolher_vencedor_decisao");
}
};

const handleConfirmarAbandono = () => {
setModalAbandonar(false);
if (!modoTreino && eventoEmAndamento) {
const novasLutas = eventoEmAndamento.lutasAgendadas.map(l => {
if (l.idLuta === idLutaAtiva) {
return {
...l,
status: "Interrompida",
dadosSalvos: { vida1, vida2, turnos1, turnos2, roundAtual, rounds, vezDeQuem }
};
}
return l;
});
setEventoEmAndamento({ ...eventoEmAndamento, lutasAgendadas: novasLutas });
}
resetarEstadosDeCombate();
setTela(modoTreino ? "menu_inicial" : "continuar_evento");
};

const resetarEstadosDeCombate = () => {
setVida1(200); setVida2(200); setTurnos1(0); setTurnos2(0); setRoundAtual(1);
setMetodoSelecionado(null); setAlvoSelecionado(null); setVezDeQuem(null); setDano("");
setPointsL1(""); setPontosL2(""); setVencDecisaoTemp(null); setRoundAcabouAviso(false);
setIdLutaAtiva(null);
};

const finalizarLuta = (metodo, vencedor, turnoVenc, roundVenc, pontosDec = null) => {
const res = {
vencedor, metodo, round: roundVenc, turno: turnoVenc,
lutador1, lutador2, tipoEventoLuta: getLabelLutaPorArrayOriginal(lutador1, lutador2),
nomeDoEvento: modoTreino ? "TREINO" : eventoEmAndamento?.nome || "EVENTO", pontosDec
};
if (!modoTreino && eventoEmAndamento) {
const novasLutas = eventoEmAndamento.lutasAgendadas.map(l => {
if (l.idLuta === idLutaAtiva) {
return { ...l, status: "Finalizada", resultado: res, dadosSalvos: null };
}
return l;
});
const novoEvento = { ...eventoEmAndamento, lutasAgendadas: novasLutas };
setEventoEmAndamento(novoEvento);
if (novasLutas.every(l => l.status === "Finalizada")) {
setHistoricoEventos(prev => [novoEvento, ...prev]);
}
}
setResultado(res);
setTela("resultado");
};

const concluirLutaEResetar = () => {
resetarEstadosDeCombate();
if (modoTreino) {
setModoTreino(false); setLutador1(null); setLutador2(null); setTela("menu_inicial");
} else {
if (eventoEmAndamento && eventoEmAndamento.lutasAgendadas.every(l => l.status === "Finalizada")) {
setEventoEmAndamento(null); setTela("menu_inicial");
} else {
setTela("continuar_evento");
}
}
};

const handleEncerrarEventoManual = () => {
if (!eventoEmAndamento) return;
const lutasCompletas = eventoEmAndamento.lutasAgendadas.filter(l => l.status === "Finalizada");
if (lutasCompletas.length > 0) {
const eventoFiltradoParaHistorico = {
...eventoEmAndamento,
lutasAgendadas: lutasCompletas
};
setHistoricoEventos(prev => [eventoFiltradoParaHistorico, ...prev]);
}
setEventoEmAndamento(null);
resetarEstadosDeCombate();
setTela("menu_inicial");
mostrarAlertaCustom("EVENTO FINALIZADO", "O evento foi encerrado. Lutas completas foram salvas e as incompletas foram apagadas.");
};

const renderTelaConteudo = () => {
if (tela === "menu_inicial") return (
<View style={styles.containerCentro}>
<View style={styles.header}>
<TouchableOpacity onPress={() => setMenuVisivel(true)} style={styles.hitboxMenu}>
<Text style={styles.tresPontos}>⋮</Text>
</TouchableOpacity>
</View>
<LogoUfcCiems />
<TouchableOpacity style={[styles.botaoMainNeon, styles.elementoLarguraPadrao]} onPress={() => { setModoTreino(false); setLutador1(null); setLutador2(null); setCategoria(null); setTela("selecao_tipo_evento"); }}>
<Text style={styles.textoBotaoMain}>CRIAR NOVO EVENTO</Text>
</TouchableOpacity>
<TouchableOpacity style={[styles.botaoSecundarioBorda, styles.elementoLarguraPadrao, {marginTop: 18}]} onPress={() => { setModoTreino(true); setLutaAtualIdx(0); setLutador1(null); setLutador2(null); setCategoria(null); setTela("agendar_lutas"); }}>
<Text style={styles.textoBotaoSecundario}>LUTA RÁPIDA (TREINO)</Text>
</TouchableOpacity>
<TouchableOpacity style={[styles.botaoSecundarioBorda, styles.elementoLarguraPadrao, {marginTop: 18}, eventoEmAndamento && styles.botaoMainAtivoAzul]} onPress={() => eventoEmAndamento ? setTela("continuar_evento") : setModalAlerta({ visivel: true, titulo: "AVISO", mensagem: "NÃO HÁ EVENTO EM ANDAMENTO!" })}>
<Text style={[styles.textoBotaoSecundario, eventoEmAndamento && {color: '#FFF'}]}>CONTINUAR EVENTO</Text>
</TouchableOpacity>
<View style={[styles.divisorLine, styles.elementoLarguraPadrao]} />
<TouchableOpacity style={[styles.botaoSecundarioBorda, styles.elementoLarguraPadrao, {borderColor: 'rgba(255,255,255,0.15)'}]} onPress={() => { setNovoLutadorNome(""); setCategoriaSelecionada(null); setTela("adicionar_lutador"); }}>
<View style={styles.rowBotaoComIcone}>
<IconeMais />
<Text style={styles.textoBotaoSecundarioBranco}>ADICIONAR LUTADOR</Text>
</View>
</TouchableOpacity>
<TouchableOpacity style={[styles.botaoSecundarioBorda, styles.elementoLarguraPadrao, {marginTop: 18, borderColor: 'rgba(255,255,255,0.15)'}]} onPress={() => { setCategoria(null); setLutadorSendoEditado(null); setTela("gerenciar_lutadores"); }}>
<View style={styles.rowBotaoComIcone}>
<IconeEngrenagem />
<Text style={styles.textoBotaoSecundarioBranco}>GERENCIAR LUTADORES</Text>
</View>
</TouchableOpacity>
</View>
);
if (tela === "adicionar_lutador") return (
<View style={styles.containerCentro}>
<Text style={styles.titulo}>NOVO LUTADOR</Text>
<TextInput style={[styles.inputPremium, styles.elementoLarguraPadrao]} placeholder="NOME DO LUTADOR" placeholderTextColor="#555" value={novoLutadorNome} onChangeText={setNovoLutadorNome} />
<Text style={[styles.nomeLabel, {textAlign: 'center', marginVertical: 15}]}>SELECIONE A CATEGORIA:</Text>
<View style={[styles.rowCategoriasTabs, styles.elementoLarguraPadrao]}>
{Object.keys(lutadoresDB || {}).m
EAS Build is also designed to work for any native project, whether or not you use Expo and React Native. It's the fastest way to get from `npx create-expo-app` or `npx @react-native-community/cli@latest init` to app stores.

## Quick start

> **info** The `eas` commands below require EAS CLI. See [How to install EAS CLI](/eas/cli/#installation) for more information.

To build your app, run the following command:

<Terminal cmd={['$ eas build --platform all']} />

This command sends your project to EAS Build and produces installable binaries for Android and iOS. You can also build for one platform at a time by passing in `--platform android` or `--platform ios` as desired. For complete setup instructions, see [Create your first build](/build/setup/).

### Expo Skills for AI agents

If you use an AI agent, install [Expo Skills](/skills/) to teach it how to create production builds and ship them to the app stores:

<RelatedSkills names={['eas-app-stores']} />

## Key features

- Cloud builds for Android and iOS with consistent environments
- Automatically provision and manage app signing credentials or use your own
- Share [internal distribution](/build/internal-distribution/) builds with a URL
- Automate builds with [build profiles](/build/eas-json/#build-profiles) in **eas.json** (named sets of build settings) and integrations with [EAS Workflows](/eas/workflows/get-started/) or [CI pipelines](/build/building-on-ci/)
- Auto-submit successful builds to app stores via [`--auto-submit`](/build/automate-submissions/) and EAS Submit
- First-class [`expo-updates` integration](/build/updates/) with per-profile channels and [runtime version](/eas-update/runtime-versions/) guidance
- Reuse [development builds](/develop/development-builds/introduction/) across your team. When two team members run `eas build:dev` and the project fingerprint matches, the existing build is downloaded from EAS instead of creating a new one
- Faster builds via [dependency caching and custom cache paths](/build-reference/caching/)
- Install builds and updates on devices with [Expo Orbit](https://expo.dev/orbit)

## When to use EAS Build

| Scenario                                                                             | Recommendation |
| ------------------------------------------------------------------------------------ | -------------- |
| Build production-ready binaries for app stores                                       | <YesIcon />    |
| Share builds with testers via [internal distribution](/build/internal-distribution/) | <YesIcon />    |
| Consistent builds across team members without local environment setup                | <YesIcon />    |
| Automate builds from CI or [EAS Workflows](/eas/workflows/get-started/)              | <YesIcon />    |
| Managed app signing credentials                                                      | <YesIcon />    |
| Debugging native code locally                                                        | <NoIcon />     |

## Frequently asked questions

<FAQ>

<Collapsible summary="How do I share builds with my team before submitting to app stores?">

Use [internal distribution](/build/internal-distribution/) to share builds with a URL. Set `"distribution": "internal"` in your [build profile](/build/eas-json/#build-profiles) in **eas.json** to generate installable Android Package (APK) files for Android or [ad hoc builds](/build/internal-distribution/) for iOS.

</Collapsible>

<Collapsible summary="Can I use EAS Build with existing React Native projects?">

Yes. EAS Build works with existing React Native projects created with `npx react-native init` or similar tools. See [Overview of using Expo with existing React Native apps](/bare/overview/) for more information.

</Collapsible>

<Collapsible summary="Does EAS Build handle app signing credentials?">

Yes. EAS Build can generate and manage Android [keystores](/app-signing/app-credentials/#android), iOS [provisioning profiles](/app-signing/app-credentials/#ios) and [distribution certificates](/app-signing/app-credentials/#ios), or use credentials you provide. See [App signing credentials](/app-signing/app-credentials/) for more information.

</Collapsible>

<Collapsible summary="Can I run builds locally instead of in the cloud?">

Yes. Use [local builds](/build-reference/local-builds/) with `eas build --local` to run builds on your machine. This is useful for debugging or for security policies that require local builds.

</Collapsible>

<Collapsible summary="Can I use EAS Build with EAS Workflows or CI pipelines?">

Yes. EAS Build integrates with [EAS Workflows](/eas/workflows/get-started/) using the `build` job type. Add a build job to your workflow configuration, for example:

```yaml
jobs:
  build_ios:
    type: build
    params:
      platform: ios
```

The build job supports builds for both platforms or conditional builds based on the branch:

```yaml
jobs:
  build:
    type: build
    params:
      platform: all
      profile: ${{ github.ref_name == 'main' && 'production' || 'preview' }}
```

For more information and other usage examples, see the [EAS Workflows build job](/eas/workflows/pre-packaged-jobs#build).

EAS Build supports [builds from GitHub](/build/building-from-github/) and [building on CI](/build/building-on-ci/) with any provider.

</Collapsible>

<Collapsible summary="What build server infrastructure does EAS Build use?">

Android builds run on Linux runners hosted in Google Cloud Platform, and iOS builds run on macOS runners hosted in Expo's macOS cloud. See [Build server infrastructure](/build-reference/infrastructure/).

</Collapsible>

</FAQ>

## Get started

<BoxLink
  title="Create your first build"
  description="It should only take a few minutes in total to get up and running for iOS and/or Android."
  href="/build/setup"
  Icon={Cube01Icon}
/>

<BoxLink
  title="Share your apps with internal testers"
  description="EAS Build can help share preview builds of your app with a single URL."
  href="/build/internal-distribution"
  Icon={Cube01Icon}
/>

<BoxLink
  title="Automate submissions"
  description="Learn how EAS Build can take your successful builds and handle uploading them to app stores automatically."
  href="/build/automate-submissions"
  Icon={Cube01Icon}
/>

<BoxLink
  title="App version management"
  description="Automate version bumps so you never have to think about them again."
  href="/build-reference/app-versions"
  Icon={Cube01Icon}
/>

<BoxLink
  title="Run builds locally or on your own infrastructure"
  description="EAS Build is a hosted service, and it can also run on your own machine, for example, to debug or to comply with company security policies."
  href="/build-reference/local-builds"
  Icon={Cube01Icon}
/>

<BoxLink
  title="Limitations"
  description="EAS Build is new and rapidly evolving, so we recommend getting familiar with the current limitations."
  href="/build-reference/limitations"
  Icon={Cube01Icon}
/>
